# Copie este arquivo para .env e preencha com os dados do seu projeto Supabase
# (Project Settings → API). A chave anônima é segura para o navegador — o
# acesso real aos dados é controlado pelas políticas de RLS no banco.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
